apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: postgres
    tier: database
    environment: prod
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: postgres